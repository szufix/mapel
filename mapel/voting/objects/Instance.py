
class Instance:

    def __init__(self, experiment_id, name, model=None, alpha=None):

        self.experiment_id = experiment_id
        self.name = name
        self.model = model
        self.alpha = alpha
